{
  "files": [
    {{#each files}}
    {
      "path": "{{../agencyId}}/{{../sourceId}}/{{getUuid @root}}/{{this.name}}",
      "content_type": "{{this.contentType}}"
    }{{#unless @last}},{{/unless}}
    {{/each}}
  ],
  "file_metadata": [
    {{#each files}}
    {
      "path": "{{../agencyId}}/{{../sourceId}}/{{getUuid @root}}/{{this.name}}",
      "source_file_id": "{{getUuid @root}}",
      "file_name": "{{this.name}}",
      "content_type": "{{this.contentType}}"
    }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}